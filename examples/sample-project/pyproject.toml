[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sample-wordcount"
version = "0.1.0"
description = "The fixture hybrid-forge exercises loop changes against."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
include = ["wordcount*"]
