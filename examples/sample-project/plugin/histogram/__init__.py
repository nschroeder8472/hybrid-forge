"""A second build, with its own manifest, its own tests, and no imports from
the package beside it. Its whole reason for existing is that the loop has to
run *this* directory's command from *this* directory.
"""
