{
  "name": "hybrid-forge",
  "displayName": "Hybrid Forge",
  "version": "0.1.0",
  "description": "Plan-and-execute coding pipeline: Claude plans and reviews, a local Qwen3.6 model executes, MemPalace persists project decisions.",
  "author": {
    "name": "NMS App Works LLC"
  },
  "keywords": [
    "local-llm",
    "plan-and-execute",
    "mempalace",
    "ollama",
    "qwen"
  ],
  "userConfig": {
    "executorBaseUrl": {
      "type": "string",
      "description": "OpenAI-compatible base URL for the local executor model (e.g. http://forge-host:11434/v1)",
      "default": "http://localhost:11434/v1"
    },
    "executorModel": {
      "type": "string",
      "description": "Model tag served by the executor host",
      "default": "qwen3.6:35b-a3b"
    },
    "memPalaceUrl": {
      "type": "string",
      "description": "URL of the centrally hosted MemPalace MCP endpoint",
      "default": "http://localhost:8787/mcp"
    }
  }
}
